/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true, // Mejora la calidad del código al detectar problemas potenciales
    // Otras configuraciones pueden ir aquí
  };
  
  export default nextConfig;
  